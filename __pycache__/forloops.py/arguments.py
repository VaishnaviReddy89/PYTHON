def my_func(fname):#fname is the arguments
    print(fname+" Refunes ")
    # When the function is called, we pass along a first name, which is used inside the function to print the full name:
my_func("email")
my_func("linus")